spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/youth_health?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:15926535879Kohqq}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      initialization-fail-timeout: -1
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 5s
  flyway:
    enabled: false

security:
  jwt:
    secret: ChangeThisJwtSecretToAtLeast32CharsForDev
    expire-seconds: 86400
